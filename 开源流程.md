# FlowGame 开源流程规划

> 目标：将当前 **flowgame-editor** 拆分为可发布的 npm 包，使接入方执行 `pnpm install` 后即可在 **Vue / React / 原生 JS** 中使用 FlowGame 工作流 UI，并与可选的后端（flowgame_python）联调。

---

## 一、现状分析

### 1.1 项目定位

| 维度 | 现状 |
|------|------|
| 包名 | `flowgame-editor`（`private: true`，不可发布） |
| 形态 | 单体 Vue 3 应用（Vite SPA） |
| 画布引擎 | `@tinyflow-ai/ui`（Svelte 实现，**框架无关**，通过 `new Tinyflow()` 挂载） |
| UI 壳层 | Vue 3 + Arco Design + 自研 ProComponent（TSX） |
| 后端 | `/api` 代理到 flowgame_python（执行、Redis 存取、Qdrant 知识库） |

### 1.2 代码分层（按可复用性）

```
┌─────────────────────────────────────────────────────────────┐
│  应用层（暂不开源为库，可作为 demo / 完整产品）                │
│  flow-editor/index.vue、FlowListPanel、知识库面板、路由等     │
├─────────────────────────────────────────────────────────────┤
│  Vue 壳层（需抽成 @flowgame/vue）                            │
│  NodeInspectorPanel、CanvasFloatingToolbar、RunProgress 等   │
├─────────────────────────────────────────────────────────────┤
│  框架无关核心（需抽成 @flowgame/core）                        │
│  custom-nodes、workflow 规则、patch-*、api/flow-game、工具函数 │
├─────────────────────────────────────────────────────────────┤
│  第三方画布（peerDependency）                                │
│  @tinyflow-ai/ui + dist/index.css                            │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 多框架兼容的关键结论

- **画布本身已支持原生 JS**：当前 `flow-editor/index.vue` 的核心就是 `new Tinyflow({ element, data, provider, customNodes })`，与 Vue 无强绑定。
- **需要额外封装的是**：自定义节点、DOM 补丁、API 客户端、以及「保存 / 试运行 / 侧栏配置」等业务 UI。
- **React**：用 `useRef` + `useEffect` 挂载/销毁 Tinyflow，业务 UI 用 React 组件重写或逐步迁移。
- **Vue**：用 `defineComponent` / `<script setup>` 包装现有逻辑，迁移成本最低。

---

## 二、开源目标与原则

### 2.1 用户侧体验（成功标准）

```bash
# Vue 项目
pnpm add @flowgame/vue @flowgame/core @tinyflow-ai/ui

# React 项目
pnpm add @flowgame/react @flowgame/core @tinyflow-ai/ui

# 原生 JS / 任意框架
pnpm add @flowgame/core @tinyflow-ai/ui
```

安装后：

1. 引入 CSS（Tinyflow + FlowGame 主题）
2. 准备一个 DOM 容器
3. 调用一行初始化（或挂载一个框架组件）
4. （可选）配置 `baseURL` 连接自有后端

### 2.2 设计原则

| 原则 | 说明 |
|------|------|
| **核心与 UI 分离** | `@flowgame/core` 零 Vue/React 依赖 |
| **peerDependencies** | `vue`、`react`、`@tinyflow-ai/ui` 由宿主项目提供，避免重复打包 |
| **可树摇** | ESM + `sideEffects` 标注 CSS |
| **后端可选** | 无后端时可本地编辑 JSON；有后端时走 execute / redis / qdrant API |
| **渐进接入** | 仅画布 → +自定义节点 → +完整编辑器壳 |

---

## 三、推荐 Monorepo 结构

采用 **pnpm workspace**，单仓库多包发布：

```
flowgame/
├── packages/
│   ├── core/                    # @flowgame/core
│   │   ├── src/
│   │   │   ├── nodes/           # custom-nodes（从 views/flow-editor/custom-nodes 迁出）
│   │   │   ├── workflow/      # normalize-*、build-workflow-*、rules
│   │   │   ├── patches/       # patch-canvas-*、patch-node-inspector-*
│   │   │   ├── api/           # flow-game API（axios 可注入）
│   │   │   └── tinyflow/      # tinyflow-flow-api、tinyflow-host
│   │   ├── package.json
│   │   └── vite.config.ts       # lib 模式构建
│   │
│   ├── vue/                     # @flowgame/vue
│   │   ├── src/
│   │   │   ├── FlowEditor.vue # 从 flow-editor/index.vue 精简
│   │   │   ├── components/    # NodeInspector、Toolbar、RunModal...
│   │   │   └── index.ts         # export FlowEditor + install 插件
│   │   └── package.json
│   │
│   ├── react/                   # @flowgame/react
│   │   ├── src/
│   │   │   ├── FlowEditor.tsx
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   └── styles/                  # @flowgame/styles（可选独立 CSS 包）
│       └── flowgame.css
│
├── apps/
│   ├── editor/                  # 当前 flowgame-editor（完整产品 demo）
│   ├── playground-vue/
│   ├── playground-react/
│   └── playground-vanilla/
│
├── pnpm-workspace.yaml
├── 开源流程.md                   # 本文档
└── README.md                    # 总入口文档
```

---

## 四、各包职责与 API 草案

### 4.1 `@flowgame/core`

**依赖**：`@tinyflow-ai/ui`、`axios`（或允许传入自定义 `fetch`）

**导出示例**：

```ts
// 初始化画布（原生 JS）
import { createFlowGameEditor } from '@flowgame/core'
import '@tinyflow-ai/ui/dist/index.css'
import '@flowgame/core/style.css'

const editor = createFlowGameEditor({
  element: document.getElementById('canvas')!,
  data: initialWorkflow,
  api: { baseURL: '/api' },           // 可选
  features: {
    customNodes: true,
    patches: ['watermark', 'minimap'], // 可配置启用哪些补丁
    redis: false,                      // 无后端时关闭
    knowledge: false
  }
})

editor.getWorkflow()
editor.destroy()
```

**从现有代码迁移清单**：

| 现有路径 | 迁入 core |
|----------|-----------|
| `views/flow-editor/custom-nodes/` | `packages/core/src/nodes/` |
| `views/flow-editor/*-node-*.ts` | `packages/core/src/nodes/` |
| `views/flow-editor/normalize-*.ts` | `packages/core/src/workflow/` |
| `views/flow-editor/build-workflow-*.ts` | `packages/core/src/workflow/` |
| `views/flow-editor/patch-*.ts` | `packages/core/src/patches/` |
| `views/flow-editor/tinyflow-*.ts` | `packages/core/src/tinyflow/` |
| `api/flow-game/` | `packages/core/src/api/` |

### 4.2 `@flowgame/vue`

**peerDependencies**：`vue@^3.3`、`@flowgame/core`、`@tinyflow-ai/ui`

**使用方式**：

```vue
<script setup lang="ts">
import { FlowEditor } from '@flowgame/vue'
import '@flowgame/vue/style.css'
</script>

<template>
  <FlowEditor
    :api-base-u-r-l="/api"
    :initial-data="workflow"
    @save="onSave"
    @run="onRun"
  />
</template>
```

**Props 建议**：`mode`（edit/view）、`redisKey`、`readonly`、`features`（与 core 对齐）。

### 4.3 `@flowgame/react`

**peerDependencies**：`react@^18`、`react-dom@^18`、`@flowgame/core`、`@tinyflow-ai/ui`

```tsx
import { FlowEditor } from '@flowgame/react'
import '@flowgame/react/style.css'

export function App() {
  return (
    <FlowEditor
      apiBaseURL="/api"
      onWorkflowChange={(wf) => console.log(wf)}
    />
  )
}
```

实现要点：`useRef` 挂 canvas，`useEffect` 调用 `createFlowGameEditor`，cleanup 时 `destroy()`。

### 4.4 原生 JS

不单独发包亦可，文档示例直接依赖 `@flowgame/core`：

```html
<div id="flow-canvas" style="height: 80vh"></div>
<script type="module">
  import { createFlowGameEditor } from '@flowgame/core'
  // ...
</script>
```

可提供 **UMD 构建**（`@flowgame/core/umd`）方便 `<script src>` 接入，作为二期。

---

## 五、构建与发布配置

### 5.1 每个包的 `package.json` 要点

```json
{
  "name": "@flowgame/core",
  "version": "0.1.0",
  "type": "module",
  "sideEffects": ["**/*.css"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./style.css": "./dist/style.css"
  },
  "files": ["dist"],
  "peerDependencies": {
    "@tinyflow-ai/ui": "^1.3.4"
  }
}
```

### 5.2 Vite Library 模式（core 示例）

- `build.lib.entry`: `src/index.ts`
- `rollupOptions.external`: `['vue', 'react', 'react-dom', '@tinyflow-ai/ui', 'axios']`
- 输出格式：`es`（主）、`cjs`（可选）
- `vite-plugin-dts` 生成类型

### 5.3 CSS 策略

| 来源 | 处理方式 |
|------|----------|
| `@tinyflow-ai/ui/dist/index.css` | 文档要求用户自行 import（peer） |
| FlowGame 补丁 / 知识库节点样式 | 打入 `@flowgame/core/style.css` 或 `@flowgame/styles` |
| Arco Design | **不打入库**；`@flowgame/vue` 将 Pro 壳层改为 **无头（headless）+ 可选主题**，或文档说明需自行引入 Arco |

> **重要**：当前编辑器强依赖 Arco（Modal、Form、Message）。开源库建议：
> - **方案 A（推荐）**：`FlowEditor` 仅含画布 + 必要浮动工具条；弹窗通过 `slots` / `render props` 交给宿主。
> - **方案 B**：提供 `@flowgame/vue-arco` 可选包，内置 Arco 完整体验（与现项目一致）。

---

## 六、后端与「仅 UI」模式

### 6.1 API 抽象

将 `src/request/flowgame.ts` 改为可配置：

```ts
export interface FlowGameClientOptions {
  baseURL: string
  getToken?: () => string | undefined
  onError?: (msg: string) => void  // 替代直接 Message.error
}

export function createFlowGameClient(options: FlowGameClientOptions)
```

### 6.2 功能开关

| 功能 | 依赖后端 | 无后端时行为 |
|------|----------|--------------|
| 画布编辑 | 否 | 正常 |
| 试运行 | 是 `/v1/flowGame/execute` | 隐藏或提示配置 baseURL |
| 流程列表 Redis | 是 | 关闭 `features.redis` |
| 知识库节点 | 是 Qdrant API | 使用 mock 选项或禁用节点 |

### 6.3 文档需单独说明

- [flowgame_python](https://github.com/xxx/flowgame_python) 仓库地址、端口、环境变量
- `vite.proxy` 与生产环境 `nginx` 反向代理示例

---

## 七、开源实施阶段（里程碑）

### 阶段 0：仓库准备（1–2 天）

- [ ] 取消根目录 `private: true`，选定 License（建议 **MIT**，注意 `@tinyflow-ai/ui` 为 **LGPL-3.0**，需在 README 标明兼容性）
- [ ] 添加 `LICENSE`、`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`
- [ ] 配置 GitHub Actions：`lint` → `typecheck` → `build packages` → `publish`（tag 触发）
- [ ] 创建 `pnpm-workspace.yaml`，把当前代码挪入 `apps/editor`

### 阶段 1：抽出 `@flowgame/core`（约 1 周）

- [ ] 迁移 custom-nodes、workflow 工具、api、patch、tinyflow 辅助函数
- [ ] 实现 `createFlowGameEditor` 统一入口
- [ ] 编写 `apps/playground-vanilla` 验证无框架运行
- [ ] 单元测试：workflow normalize、variable tree、run plan

### 阶段 2：发布 `@flowgame/vue`（约 1 周）

- [ ] 从 `flow-editor/index.vue` 拆出 `FlowEditor.vue`
- [ ] 解耦 Arco：优先 slots + 事件（`@save`、`@run`、`@open-knowledge`）
- [ ] 提供 `apps/playground-vue` 最小示例
- [ ] 文档：Vue 3 + Vite 接入指南

### 阶段 3：发布 `@flowgame/react`（约 1–2 周）

- [ ] 实现 `FlowEditor` React 组件（逻辑复用 core，UI 用 React 重写或 `@arco-design/web-react` 可选）
- [ ] `apps/playground-react`
- [ ] 文档：React 18 + Vite 接入指南

### 阶段 4：文档与 npm 首发（约 3 天）

- [ ] 根 README：架构图、快速开始、三种框架 Tab 示例
- [ ] 站点（VitePress / Storybook）展示 live demo
- [ ] 发布 `@flowgame/core@0.1.0`、`@flowgame/vue@0.1.0`
- [ ] 将原 `flowgame-editor` 改为依赖 workspace 包的 demo 应用

### 阶段 5：生态增强（持续）

- [ ] UMD / CDN 文档
- [ ] `@flowgame/vue-arco` 完整版（对齐现网体验）
- [ ] CLI：`pnpm create flowgame` 脚手架
- [ ] 国际化 i18n
- [ ] 自定义节点注册文档与 TypeScript 类型导出

---

## 八、接入方快速开始（目标文档内容）

### 8.1 Vue 3

```bash
pnpm add @flowgame/vue @flowgame/core @tinyflow-ai/ui
```

```ts
// main.ts
import '@tinyflow-ai/ui/dist/index.css'
import '@flowgame/vue/style.css'
```

```vue
<FlowEditor api-base-u-r-l="https://your-api.com/api" />
```

### 8.2 React 18

```bash
pnpm add @flowgame/react @flowgame/core @tinyflow-ai/ui
```

```tsx
<FlowEditor apiBaseURL="https://your-api.com/api" />
```

### 8.3 原生 JS

```bash
pnpm add @flowgame/core @tinyflow-ai/ui
```

```ts
import { createFlowGameEditor } from '@flowgame/core'
```

### 8.4 开发期代理（与现项目一致）

```ts
// vite.config.ts
server: {
  proxy: {
    '/api': { target: 'http://127.0.0.1:8008', changeOrigin: true }
  }
}
```

---

## 九、许可证与合规

| 依赖 | 许可证 | 开源注意 |
|------|--------|----------|
| `@tinyflow-ai/ui` | LGPL-3.0-or-later | 若静态链接/打包分发，需遵守 LGPL；动态 import 与 peerDependency 方式风险较低，**务必法务确认** |
| `@arco-design/web-vue` | MIT | 可选包使用 |
| `@xyflow/svelte`（Tinyflow 传递） | MIT | 随 Tinyflow 使用 |

建议在 README 增加 **Third-Party Licenses** 章节，并考虑 core 仅 peer 依赖 Tinyflow、不将其打入 bundle。

---

## 十、质量与维护

### 10.1 CI 检查项

- `pnpm -r typecheck`
- `pnpm -r build`
- playground 三端 smoke test（挂载不报错）
- Changesets 版本管理（`pnpm changeset`）

### 10.2 版本策略

- **0.x**：API 可 Breaking，快速迭代
- **1.0**：`createFlowGameEditor` 与三端 `FlowEditor` props 稳定
- 与 `@tinyflow-ai/ui` 主版本对齐说明（如 `^1.3`）

### 10.3 Issue 模板

- 🐛 Bug
- ✨ Feature（自定义节点 / 新后端适配）
- 📖 文档
- 🧩 框架接入求助（vue/react/vanilla）

---

## 十一、当前仓库 → 开源包映射表

| 现有文件/目录 | 目标包 | 优先级 |
|---------------|--------|--------|
| `src/views/flow-editor/custom-nodes/` | `@flowgame/core` | P0 |
| `src/views/flow-editor/tinyflow-*.ts` | `@flowgame/core` | P0 |
| `src/views/flow-editor/patch-*.ts` | `@flowgame/core` | P0 |
| `src/views/flow-editor/normalize-*.ts` | `@flowgame/core` | P0 |
| `src/views/flow-editor/build-workflow-*.ts` | `@flowgame/core` | P0 |
| `src/api/flow-game/` | `@flowgame/core` | P0 |
| `src/views/flow-editor/index.vue` | 拆分到 core 逻辑 + `@flowgame/vue` | P0 |
| `src/views/flow-editor/*.vue`（子组件） | `@flowgame/vue` | P1 |
| `src/components/ProComponent/` | 不进入首版开源（或 `@flowgame/vue-arco`） | P2 |
| `src/views/FlowKnowledgePanelContent.vue` | `@flowgame/vue` feature 包 | P2 |
| `apps/editor`（整应用） | 保留为官方 Demo | P0 |

---

## 十二、风险与对策

| 风险 | 对策 |
|------|------|
| 单体 Vue 代码与 Arco 耦合深 | 首版 core 无 UI 库；vue 包用 slots 剥离 Modal |
| Tinyflow LGPL 限制 | peerDependency + 文档说明；必要时联系 tinyflow 作者 |
| 补丁依赖 Shadow DOM 结构 | 补丁版本与 `@tinyflow-ai/ui` 版本锁定范围写进 peerDeps |
| 后端 API 变更 | `api/flow-game` 集中 + OpenAPI 生成类型（可选） |
| 用户期望 `pnpm install` 即可跑全功能 | README 区分 **UI only** 与 **UI + Python 后端** 两套 Quick Start |

---

## 十三、检查清单（发布前）

- [ ] 根包可 `pnpm install && pnpm -r build`
- [ ] playground-vue / react / vanilla 均可 `pnpm dev` 看到画布
- [ ] npm 包 `files` 仅含 `dist`，不含 `src`
- [ ] 类型声明完整（`d.ts`）
- [ ] README 含 Vue / React / Vanilla 三份最小示例
- [ ] CHANGELOG 与 Git tag 一致
- [ ] LGPL / MIT 声明清晰

---

## 十四、总结

1. **画布层**已由 `@tinyflow-ai/ui` 实现跨框架；FlowGame 的开源重点是抽出 **`@flowgame/core`**（节点、规则、API、补丁）及 **Vue/React 薄封装**。
2. 采用 **pnpm monorepo** 多包发布，用户按框架安装对应包，统一 `pnpm install` 体验。
3. 分五期实施：仓库准备 → core → vue → react → 文档与 npm 首发；原 `flowgame-editor` 变为官方完整 Demo。
4. 首版追求 **「能嵌入、能编辑、能接后端」**；Arco 完整壳层、知识库管理页可作为可选包后续迭代。

---

*文档版本：v1.0 · 基于 flowgame-editor 仓库现状整理*
