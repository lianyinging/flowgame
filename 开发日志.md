# FlowGame 开发日志

本文件记录 monorepo 与相关仓库的功能演进，供团队与开源协作者查阅。  
**维护约定**：每次合并有意义的功能或行为变更时，在顶部追加一节（见 [.cursor/skills/flowgame-project/SKILL.md](.cursor/skills/flowgame-project/SKILL.md)）。

---

## 2026-05-29 — Docker 部署文档与编排文件

- **做了什么**：新增 `deploy/`（`docker-compose.yml`、前端 `Dockerfile`、`nginx.conf`、`.env.example`）；`flowgame_python` 增加后端 `Dockerfile`；编写 [Docker部署.md](Docker部署.md)，README 增加「Docker 部署」章节。
- **涉及**：`deploy/`、`Docker部署.md`、`README.md`；后端 `flowgame_python/Dockerfile`
- **验证**：服务器上并列克隆两仓库后，在 `flowgame/deploy` 执行 `docker compose up -d --build`，访问 `:8009` 与 `/health`。

---

## 2026-05-27 — 文档与项目 Skill 初始化

- **做了什么**：建立 `开发日志.md`；扩充根目录 `README.md`（保姆级接入说明）；新增 Cursor Skill `flowgame-project`，约定三仓库路径与文档同步更新流程。
- **涉及**：`README.md`、`开发日志.md`、`.cursor/skills/flowgame-project/SKILL.md`
- **验证**：按 README「快速开始」与「验证打包」章节操作即可。

---

## 2026-05-24 — Monorepo 与可发布包 v0.1.0

- **做了什么**：将编辑器拆为 `@flowgame/core`、`@flowgame/vue`；提供 `flowgame-editor`、`playground-vue`；支持 `pnpm pack:packages` 生成 tgz。
- **涉及**：`packages/core`、`packages/vue`、`apps/editor`、`CHANGELOG.md`
- **验证**：`pnpm install && pnpm dev` → http://127.0.0.1:8009

---

## 进行中（未发版）— 记忆节点与 HTML 模板节点

> 以下变更见当前工作区，发版后请把本节改为正式日期条目，并更新 `CHANGELOG.md`。

- **记忆节点**：`node-memory-read`、`node-memory-write`；参数与画布展示（`memory-node-parameters.ts`、`memory-write-canvas.ts`）；工作流归一化 `normalize-memory-node-params.ts`、`memory-write-groups.ts`。
- **HTML 模板节点**：`node-html-template`；预览与 iframe（`html-template-canvas-preview.ts`、`html-preview-iframe.ts` 等）；Vue 侧 `HtmlTemplateEditorWithPreview.vue`、`HtmlTemplateInputParametersBlock.vue`。
- **编辑器增强**：工具栏变量补丁、知识库/流程列表面板（`FlowKnowledgePanelModal`、`FlowListPanelModal`）、ProComponent 表格/表单/抽屉。
- **后端**：`flowgame_python` 中 `memory_context`、链节点对 memory 读写需与前端节点 type 对齐后再做联调说明。

---

## 仓库索引

| 名称 | 路径 |
|------|------|
| 前端 Monorepo | `/Users/lianying/Desktop/ai工作流/flowgame` |
| Python 执行器 | `/Users/lianying/Desktop/ai工作流/flowgame_python` |
| 打包测试项目 | `/Users/lianying/Desktop/ai工作流/前端测试打包效果/flowgame-test` |
