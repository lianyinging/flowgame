# 接入测试指南（npm + 本地 tgz）

`@flowgame/core`、`@flowgame/vue` 已发布到 **npm**（当前 **0.1.0**）。  
第三方项目与 `flowgame-test` 应优先从 registry 安装；发版前维护者仍可用 `pnpm pack` 做本地验证。

```bash
npm view @flowgame/core version
npm view @flowgame/vue version
```

---

## 一、从 npm 安装（推荐）

### 1. 创建 Vue 3 项目

```bash
pnpm create vite my-flowgame-test --template vue-ts
cd my-flowgame-test
pnpm install
```

### 2. 安装 FlowGame 与 peer 依赖

```bash
pnpm add @flowgame/vue @flowgame/core @tinyflow-ai/ui @arco-design/web-vue vue
```

### 3. 配置项目

**`src/main.ts`**

```ts
import { createApp } from 'vue'
import ArcoVue from '@arco-design/web-vue'
import '@arco-design/web-vue/dist/arco.css'
import '@tinyflow-ai/ui/dist/index.css'
import '@flowgame/vue/style.css'
import { configureFlowGameClient } from '@flowgame/core'
import App from './App.vue'

configureFlowGameClient({
  baseURL: '/api',
  onError: (msg) => alert(msg)
})

createApp(App).use(ArcoVue).mount('#app')
```

**`src/App.vue`**

```vue
<script setup lang="ts">
import { FlowEditor } from '@flowgame/vue'
</script>

<template>
  <FlowEditor class="flow-editor-host" />
</template>

<style>
html, body, #app { margin: 0; height: 100%; overflow: hidden; }
.flow-editor-host { display: block; width: 100%; height: 100%; }
</style>
```

**`vite.config.ts`**（需要后端时）

```ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    proxy: {
      '/api': {
        target: 'http://127.0.0.1:8008',
        changeOrigin: true
      }
    }
  }
})
```

### 4. 运行

```bash
pnpm dev
```

### 5. 发新版后更新测试项目

```bash
cd /path/to/flowgame-test
pnpm update @flowgame/core @flowgame/vue
rm -rf node_modules/.vite
pnpm dev
```

---

## 二、本地 tgz（发版前维护者）

在正式发布到 npm 之前，用 `pnpm pack` 生成 tarball 验证。

### 1. 在 flowgame 仓库打包

```bash
cd /path/to/flowgame
pnpm install
pnpm pack:packages
```

成功后会得到：

- `packages/core/flowgame-core-0.1.0.tgz`
- `packages/vue/flowgame-vue-0.1.0.tgz`

### 2. 安装本地 tgz

```bash
pnpm add /path/to/flowgame/packages/core/flowgame-core-0.1.0.tgz \
         /path/to/flowgame/packages/vue/flowgame-vue-0.1.0.tgz
```

其余配置同上一节。

---

## 常见问题

| 现象 | 处理 |
|------|------|
| 找不到 `@flowgame/core` | 同时安装 `@flowgame/core` 与 `@flowgame/vue` |
| 画布无样式 / 节点面板图标巨大 | 确认引入 `@tinyflow-ai/ui/dist/index.css` 与 `@flowgame/vue/style.css`；npm 上升级到最新版 |
| 装了新版本但行为未变 | 删 `node_modules/.vite` 后重启 `pnpm dev` |
| 控制台 `form-data` 无 default export | 勿随意 `optimizeDeps.exclude` @flowgame 包；清 `.vite` 缓存 |
| 试运行失败 | 启动 flowgame_python 并配置 Vite 代理 |
| Arco 组件报错 | `app.use(ArcoVue)` 且引入 `arco.css` |
| 「流程列表」「知识库配置」只出现 Message | 升级 `@flowgame/vue` 并确保后端 `/api` 可用 |

### 测试项目仅更新 core（发版前 tgz 场景）

```bash
cd /path/to/flowgame && pnpm -C packages/core build && pnpm -C packages/core pack
cd /path/to/flowgame-test
pnpm add /path/to/flowgame/packages/core/flowgame-core-0.1.0.tgz
rm -rf node_modules/.vite && pnpm dev
```

---

## 回到 flowgame 继续开发

测试项目与 monorepo 无关；你在 `flowgame` 里照常 `pnpm dev` 即可。

