# 本地模拟 `pnpm add` 测试指南

在正式发布到 npm 之前，用 `pnpm pack` 生成 tarball，在新 Vue 项目中验证。

## 1. 在 flowgame 仓库打包

```bash
cd /path/to/flowgame
pnpm install
pnpm pack:packages
```

成功后会得到：

- `packages/core/flowgame-core-0.1.0.tgz`
- `packages/vue/flowgame-vue-0.1.0.tgz`

## 2. 创建新的 Vue 3 项目

```bash
pnpm create vite my-flowgame-test --template vue-ts
cd my-flowgame-test
pnpm install
pnpm add vue @arco-design/web-vue @tinyflow-ai/ui
```

## 3. 安装本地 tgz

将路径换成你机器上的绝对路径：

```bash
# 必须一次安装两个 tgz（core 未上 npm，不能单独装 vue）
pnpm add /path/to/flowgame/packages/core/flowgame-core-0.1.0.tgz /path/to/flowgame/packages/vue/flowgame-vue-0.1.0.tgz
```

## 4. 配置项目

**`src/main.ts`**

```ts
import { createApp } from 'vue'
import ArcoVue from '@arco-design/web-vue'
import '@arco-design/web-vue/dist/arco.css'
import '@tinyflow-ai/ui/dist/index.css'
import '@flowgame/vue/style.css'
import { configureFlowGameClient } from '@flowgame/core'
import App from './App.vue'

configureFlowGameClient({
  baseURL: '/api',
  onError: (msg) => alert(msg)
})

createApp(App).use(ArcoVue).mount('#app')
```

**`src/App.vue`**

```vue
<script setup lang="ts">
import { FlowEditor } from '@flowgame/vue'
</script>

<template>
  <FlowEditor class="flow-editor-host" />
</template>

<style>
html, body, #app { margin: 0; height: 100%; overflow: hidden; }
.flow-editor-host { display: block; width: 100%; height: 100%; }
</style>
```

**`vite.config.ts`**（需要后端时）

```ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    proxy: {
      '/api': {
        target: 'http://127.0.0.1:8008',
        changeOrigin: true
      }
    }
  }
})
```

## 5. 运行

```bash
pnpm dev
```

## 常见问题

| 现象 | 处理 |
|------|------|
| 找不到 `@flowgame/core` | 同时安装 core 的 tgz |
| 画布无样式 / 节点面板图标巨大 | ① 确认引入 `@tinyflow-ai/ui/dist/index.css` 与 `@flowgame/vue/style.css`；② 重新 `pnpm pack:packages` 安装（旧 tgz 的 style.css 可能只有 9KB） |
| 试运行失败 | 启动 flowgame_python 并配置 Vite 代理 |
| Arco 组件报错 | `app.use(ArcoVue)` 且引入 `arco.css` |

## 6. 回到 flowgame 继续开发

测试项目与 monorepo 无关；你在 `flowgame` 里照常 `pnpm dev` 即可。
